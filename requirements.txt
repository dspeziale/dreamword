# requirements.txt
# Database Framework Dependencies

# Core dependencies (sempre necessarie)
# Nessuna dipendenza esterna per SQLite (built-in Python)

# MySQL support (opzionale)
mysql-connector-python>=8.0.0

# SQL Server support (opzionale)
pyodbc>=4.0.0

# Development dependencies (opzionale)
pytest>=7.0.0
pytest-cov>=4.0.0

# Documentation (opzionale)
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0